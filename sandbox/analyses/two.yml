description: two
# specifying a dependency or simply specifying which field(s)
# you need are both supported
#dependencies:
#    - one
requirements:
    - title
environment:
    python:
        - lxml
    apt:
        - ffmpeg
schema:
    orig-title: text
    facebook:
        shares: integer
        likes: integer